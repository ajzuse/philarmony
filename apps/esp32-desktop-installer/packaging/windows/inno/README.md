# Inno Setup (optional)

See `philarmony-installer.iss`. Compile with Inno Setup 6 after `flutter build windows --release`.
Output defaults to `apps/esp32-desktop-installer/dist/PhilarmonyInstaller-Setup-0.1.0.exe`.
